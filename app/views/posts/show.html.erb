<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>

  <h4>Content:</h4>
  <%= @post.content %>

  <p>Tags:</p><% @post.tags.each do |t|%>
    <ul><%= t.name %></ul>
  <% end %> 
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
